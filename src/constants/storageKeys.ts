/**
 * Storage Keys Enum
 * Centralized enum for all AsyncStorage and local storage keys
 * Provides better type safety and maintainability
 */
export enum StorageKeysEnum {
  USER_SESSION = 'user_session',
  FILTERS_STORAGE_KEY = 'task_filters',
}
